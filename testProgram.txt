*- comment normal-*

*- *- comment anidado -* -*

init {
    x, z, a, b, c, var_entera : Int
    variable1, var_flotante : Float
    y, p1, var_cadena : String
}

*- test -*
mientras (x > y) {
    x := 1
}